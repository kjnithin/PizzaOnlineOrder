#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../flow-parser/tools/inspect_ast.js" "$@"
  ret=$?
else 
  node  "$basedir/../flow-parser/tools/inspect_ast.js" "$@"
  ret=$?
fi
exit $ret
